# DAA Assignment 1: Divide and Conquer

Implementation of MergeSort, QuickSort, and QuickSelect in Java with bounded recursion depth and memory optimization[cite: 1].

## Requirements
* Java JDK 17+[cite: 1]
* Apache Maven[cite: 1]

## Commands

1. **Run JUnit 5 tests:**
   ```bash
   mvn test

2. Run benchmark (generates results.csv):
   ```bash
   mvn exec:java -Dexec.mainClass="daa.BenchmarkRunner"
